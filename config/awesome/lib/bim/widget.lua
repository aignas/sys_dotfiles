-- This will contain any widget or feedback code, which is needed to run the library
-- A way to do this is using naughty.notify.
-- However, we can as well build a floating wibox on top, which can be updated easier.

function update(wibox)
    put_into_wibox_what_is_needed.
end

Connect_stuff_with_signals
